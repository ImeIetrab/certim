Circular initials avatar — sidebar user block, account header.

```jsx
<Avatar initials="A" />
<Avatar initials="AD" size={44} />
```

- Blue-mid fill with cream initials. `size` scales the font automatically.
